{% extends getPartial('page-design') %}

{% block PartialHead %}
    {% set pageMetadata = pageMetadata.withTitle("Ceres::Template.myAccountChangeEmail") %}
{% endblock %}

{% block PageBody %}
    {{ component("Ceres::Customer.Components.ChangeEmailForm") }}

    <change-email-form
            :contact-id="{{ contactId }}"
            hash="{{ hash }}"
            new-mail="{{ newMail }}">
    </change-email-form>

{% endblock %}
